#include <string.h>
#include <stdio.h>
#include <stdlib.h>

int main()
{
    int mul;
    int num1;
    int num2;

    scanf("%d", &num1);
    scanf("%d", &num2);

    mul = num1 * num2;

    printf("%d", mul);

}