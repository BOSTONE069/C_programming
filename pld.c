#include <stdio.h>

#define PI 3.14

int main ()
{
  const float PI = 3.14
   int x = 2;
   int y;
   char c;
   float z;
:
   // Arithmetic operators + - * / % ++ --
   // Relational operators == != < > <= >=
   // Logical operators AND(&&) OR(||) NOT(!)
   // Bitwise operators & | ^ ~ << >>
   // Asssignment operators = += -= *= /=
   //Misc  operators sizeof ?:
   //Conditional operators ?:
   

   //Control Statements
   //1- Conditional control if, if--else,  nested if, switch,  if..elseif.. else
   //2- Looping control for, while, do..while

  /*Printing hello world*/
 
  scanf(""); // This is used in getting the values from the user
  printf("Hello World\n");//This is used to get an output 

 return (0);
}
