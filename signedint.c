#include <stdio.h>

/**
 * The function `printf` prints the value of `s` in decimal and unsigned decimal format
 *
 * @return -1 and 4294967295
 */
int main ()
{
	int s = -1;

	printf("\t %d",s);
	printf("\t %u",s);

	return 0;
}
